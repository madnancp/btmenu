[project]
name = "btmenu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "dbus-next>=0.2.3",
    "typer>=0.26.1",
]

[project.scripts]
btmenu = "btmenu.main:main"

[tool.uv]
package = true
